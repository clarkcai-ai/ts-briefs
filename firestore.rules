rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporary: wide open. Anyone with the URL can read/write.
      // TODO: re-enable @tubescience.com gating once the team is unblocked.
      allow read, write: if true;
    }
  }
}
